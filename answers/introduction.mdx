---
title: "What are Answers?"
description: "Understanding Semilattice's answer prediction system"
---

Answers are the core output of Semilattice's prediction engine. When you ask a population a question, you get back an Answer object that contains the predicted response distribution for that specific group.

## Key Concepts

### Population-Level Predictions
Answers represent how a **population** would respond to questions, not individual people. You'll never see simulated responses from specific individuals - only aggregated percentages showing how the group as a whole would distribute across answer options.

```json
{
  "simulated_answer_percentages": {
    "Very satisfied": 0.34,
    "Somewhat satisfied": 0.41,
    "Neutral": 0.15,
    "Somewhat dissatisfied": 0.07,
    "Very dissatisfied": 0.03
  }
}
```

### Answer Management
The **Answers** section in your dashboard shows all questions you've simulated across different populations. Each row represents a unique question-population combination, allowing you to track and compare predictions over time.

### Question Types
The API supports three types of questions:

<CardGroup cols={3}>
  <Card title="Single-Choice" icon="circle-dot">
    Respondents select exactly one option from a list of choices.
  </Card>
  <Card title="Multiple-Choice" icon="list-check">
    Respondents can select multiple options from a list of choices.
  </Card>
  <Card title="Open-Ended" icon="message">
    Respondents provide free-text answers. **Currently in alpha!**
  </Card>
</CardGroup>

## Two Types of Answer Prediction

### Simulate Answers
When you want to predict how a population would respond to **new questions**:
- Use the `answers.simulate()` method
- Accuracy can only be estimated from the population's test results
- Perfect for market research, product decisions, and audience insights

### Benchmark Answers  
When you want to **test a population** against questions with known answers:
- Use the `answers.benchmark()` method  
- Compares predictions to ground truth data
- Generates accuracy metrics to evaluate population performance
- Essential for validating population quality

## Response Accuracy

### Simulated Answers
For new questions, accuracy depends on:
- **Population quality**: How well the population was built
- **Question similarity**: How closely your question matches the seed data
- **Population evaluation scores**: The metrics from population testing

### Benchmark Answers
For benchmark questions, you get:
- **Direct accuracy metrics**: MAE, RMSE, KL Divergence calculated for this specific question
- **Ground truth comparison**: See exactly how predictions compare to reality
- **Population validation**: Test whether your population is suitable for your use case

## Question Format Examples

### Single-Choice
```json
{
  "question": "What's your primary programming language?",
  "question_options": { "question_type": "single-choice" },
  "answer_options": ["Python", "JavaScript", "Java", "Go", "Rust"]
}
```

### Multiple-Choice
```json
{
  "question": "Which development tools do you use regularly?",
  "question_options": { "question_type": "multiple-choice" },
  "answer_options": ["VS Code", "Git", "Docker", "Kubernetes", "AWS"]
}
```

### Open-Ended
```json
{
  "question": "What's the biggest challenge in software development?",
  "question_options": { "question_type": "open-ended" }
}
```

## Answer Response Structure

All answer predictions follow a consistent structure, whether simulated or benchmarked:

```json
{
  "data": {
    "id": "answer-uuid",
    "population": "population-uuid",
    "question": "Your question text",
    "answer_options": ["Option 1", "Option 2"],
    "status": "Predicted",
    "simulated_answer_percentages": { "Option 1": 0.6, "Option 2": 0.4 },
    "ground_answer_percentages": null, // Only populated for benchmarks
    "accuracy": null, // Only populated for benchmarks
    // ... other fields
  }
}
```

## Next Steps

<CardGroup cols={2}>
  <Card title="Simulate an Answer" icon="crystal-ball" href="/answers/simulate-answer">
    Predict how populations respond to new questions
  </Card>
  <Card title="Benchmark an Answer" icon="chart-line" href="/answers/benchmark-answer">
    Test population accuracy against known data
  </Card>
</CardGroup>